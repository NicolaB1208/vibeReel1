[project]
name = "vibereel1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=2.3",
    "httpx>=0.27",
    "pytest>=8.0",
    "python-dotenv>=1.0",
]
